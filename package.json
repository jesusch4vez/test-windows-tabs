{
  "name": "test-windows-tabs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "foreach": "yarn workspaces foreach -ivA run",
    "bootstrap": "yarn foreach install && yarn foreach build",
    "serve": "yarn foreach start"
  },
  "workspaces": [
    "ui/",
    "orch/"
  ],
  "packageManager": "yarn@4.11.0"
}
